using System;

namespace Ejercicios.Clase_2
{
    internal class Ejercicio1
    {
        /*
        static void Main()
        {
            double PI = Math.PI;

            Console.Write("Ingrese el radio de un circulo: ");
            string input = Console.ReadLine();
            double radio = double.Parse(input);
            if (radio <= 0)
            {
                Console.WriteLine("Error, el radio ingresado debe ser positivo");
            }
            else
            {
                Console.WriteLine("El area del circulo ingresado es: " + calcularArea(radio));
                Console.WriteLine("El perimetro del circulo ingresado es: " + calcularPerimetro(radio));
            }

            double calcularArea(double radio)
            {
                return radio * radio;
            }

            double calcularPerimetro(double radio)
            {
                return (2 * PI) * radio;
            }
        }
        */

    }
}